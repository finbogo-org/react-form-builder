const AccordionItem = ({
                         title,
                         children
                       }) => {
  return null; // This component doesn't render anything directly
};

export default AccordionItem;
